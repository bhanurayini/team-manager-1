<%= form_for(team_has_coach) do |f| %>
  <% if team_has_coach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team_has_coach.errors.count, "error") %> prohibited this team_has_coach from being saved:</h2>

      <ul>
      <% team_has_coach.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :coach_type %>
    <%= f.text_field :coach_type %>
  </div>

  <div class="field">
    <%= f.label :person_id %>
    <%= f.text_field :person_id %>
  </div>

  <div class="field">
    <%= f.label :team_id %>
    <%= f.text_field :team_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
